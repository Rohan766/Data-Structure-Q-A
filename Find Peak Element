/**
 * @param {number[]} nums
 * @return {number}
 */
var findPeakElement = function(array1) {   
    let max = Math.max(...array1)
    
    for(let i=0; i<array1.length; i++){
        if(array1[i]==max){
            return i;
        }
    }
};
